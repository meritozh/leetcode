/**
 * Description:
 *
 * You are given an `n x n` 2D matrix representing an image.
 *
 * Rotate the image by 90 degrees (clockwise).
 * Follow up: Could you do this in-place?
 *
 */

#include <vector>
#include <cassert>

class Solution {
public:
  void RotateImage(std::vector<int>& Matrix) {

  }
};

int main() {
  std::vector<int> In = {};
  Solution().RotateImage(In);
  // assert();
}